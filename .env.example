# Frontend environment. Copy to .env to override the defaults.

# Where the browser sends API calls. "/api" uses the Vite dev proxy below.
VITE_API_URL=/api

# Where the Vite dev proxy forwards /api requests (the Express server).
VITE_API_PROXY=http://localhost:4000
